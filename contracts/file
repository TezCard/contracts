
Factory 合约 ：
1. create_organization（entry point） create合约
2. add_factor (entry_point) 添加因子
3. pause_factor(entry_point, offchain_view) 暂停因子
4. list_factors(offchain_view) 查询因子
5. list_organization(offchain_view) 查询合约


Organization合约
1.create_soul_bottle(entry_point)
2.create_madel_rank(entry_point)
3. start_madel_rank(entry_point)
4. join_madel_rank(entry_point)
5. receive_madel_rank_score(entry_point)
6. madel_rank_details(offchain_view)
7. list_my_participated_ranks(offchain_view)
8. list_my_madels(offchain_view)
9. is_madel_rank_open(offchain_view)
10. is_madel_rank_open(offchain_view)
11. organization_details(offchain_view)
